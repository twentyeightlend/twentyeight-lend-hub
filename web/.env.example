# Optional dedicated HyperEVM RPC (supports getLogs / higher rate limits → enables deposit
# auto-discovery). If unset, the app uses free public RPCs (auto-discovery may be limited).
# Put your real keyed URL in web/.env.local (gitignored) — never commit a keyed URL.
VITE_RPC_URL=
